'use strict'

const name = 'kyaw kyaw',
    age = 32,
    address = 'No 123, Wall str, Yangon'

// es5 
const person = {
    name: name,
    age: age,
    address: address
}

// es6
const person = { name, age, address}